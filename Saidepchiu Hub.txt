-- Saidepchiu Hub - By Biendepzai
-- Version: 2026 Ultimate (Level 2800, Control Update + Tiger Update + Christmas Event)
-- Anti-Ban System: Always Active (Kể cả khi tắt menu)

local Fluent = loadstring(game:HttpGet("https://github.com/dawid-scripts/Fluent/releases/latest/download/main.lua"))()
local Player = game:GetService("Players").LocalPlayer
local Workspace = game:GetService("Workspace")
local RunService = game:GetService("RunService")
local VirtualUser = game:GetService("VirtualUser")
local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local HttpService = game:GetService("HttpService")
local CoreGui = game:GetService("CoreGui")
local TweenService = game:GetService("TweenService")
local UserInputService = game:GetService("UserInputService")
local Stats = game:GetService("Stats")
local NetworkClient = game:GetService("NetworkClient")
local TeleportService = game:GetService("TeleportService")

-- ==================== ANTI-BAN SYSTEM (LUÔN CHẠY NGẦM) ====================
local AntiBan = {
    Connections = {},
    OriginalIndexes = {},
    NetworkProxy = nil,
    PingSpoof = nil
}

function AntiBan:Init()
    -- 1. Spoof Network Ping (Giảm phát hiện fly/TP)
    local oldPing = Stats.Network.ServerStatsItem["Data Ping"]:GetValue()
    self.PingSpoof = RunService.Heartbeat:Connect(function()
        pcall(function()
            local newPing = math.random(40, 80)
            Stats.Network.ServerStatsItem["Data Ping"]:SetValue(newPing)
        end)
    end)
    
    -- 2. Fake Network Traffic (Làm nhiễu log)
    for i = 1, 5 do
        self.Connections[i] = RunService.Heartbeat:Connect(function()
            pcall(function()
                ReplicatedStorage:WaitForChild("Remotes"):WaitForChild("__mock"):FireServer("keepalive")
            end)
        end)
    end
    
    -- 3. Anti-Kick (Chặn lệnh kick từ server)
    local oldNamecall
    oldNamecall = hookmetamethod(game, "__namecall", function(self, ...)
        local args = {...}
        local method = getnamecallmethod()
        
        if method == "Kick" then
            return nil
        end
        if method == "FireServer" and tostring(self):find("Teleport") then
            return nil
        end
        
        return oldNamecall(self, ...)
    end)
    
    -- 4. Spoof Character Movement (Tránh phát hiện speed hack)
    local oldWalkSpeed = game:GetService("Players").LocalPlayer.Character.Humanoid.WalkSpeed
    self.Connections[6] = RunService.Heartbeat:Connect(function()
        pcall(function()
            if Player.Character and Player.Character:FindFirstChild("Humanoid") then
                if Player.Character.Humanoid.WalkSpeed > 24 then
                    Player.Character.Humanoid.WalkSpeed = oldWalkSpeed
                end
            end
        end)
    end)
    
    -- 5. Fake Chat Spam (Làm nhiễu log chat)
    self.Connections[7] = RunService.Heartbeat:Connect(function()
        pcall(function()
            local rnd = math.random(1, 100)
            if rnd == 1 then
                ReplicatedStorage:WaitForChild("DefaultChatSystemChatEvents"):WaitForChild("SayMessageRequest"):FireServer("/hub", "All")
            end
        end)
    end)
    
    -- 6. Anti-Log (Xóa log hành động)
    local oldLog = debug.getregistry().lua_print
    debug.getregistry().lua_print = function(...)
        local args = {...}
        for _, v in pairs(args) do
            if type(v) == "string" and (v:find("Teleport") or v:find("Speed") or v:find("Anti") or v:find("Exploit")) then
                return nil
            end
        end
        return oldLog(...)
    end
    
    -- 7. Random Delay Between Actions (Tránh pattern)
    local oldWait = wait
    wait = function(t)
        if t then
            t = t + math.random(-50, 50)/1000
            if t < 0 then t = 0.01 end
        end
        return oldWait(t)
    end
    
    -- 8. Spoof FPS (Tránh phát hiện)
    self.Connections[8] = RunService.RenderStepped:Connect(function()
        pcall(function()
            local fps = math.random(55, 65)
            setfps(fps)
        end)
    end)
    
    -- 9. Clean Memory (Xóa dấu vết)
    self.Connections[9] = RunService.Stepped:Connect(function()
        pcall(function()
            collectgarbage()
        end)
    end)
    
    print("✅ Anti-Ban System Activated (Running 24/7)")
end

function AntiBan:Cleanup()
    for _, conn in pairs(self.Connections) do
        conn:Disconnect()
    end
    if self.PingSpoof then self.PingSpoof:Disconnect() end
end

-- ==================== NÚT ON/OFF ====================
local ScreenGui = Instance.new("ScreenGui")
local ToggleBtn = Instance.new("TextButton")
local UICorner = Instance.new("UICorner")
local UIStroke = Instance.new("UIStroke")
local Shadow = Instance.new("ImageLabel")

ScreenGui.Name = "Saidepchiu_UI"
ScreenGui.Parent = CoreGui
ScreenGui.DisplayOrder = 999
ScreenGui.ResetOnSpawn = false
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

ToggleBtn.Name = "ControlBtn"
ToggleBtn.Parent = ScreenGui
ToggleBtn.BackgroundColor3 = Color3.fromRGB(0, 255, 127)
ToggleBtn.Position = UDim2.new(0, 15, 0.5, -35)
ToggleBtn.Size = UDim2.new(0, 70, 0, 70)
ToggleBtn.Font = Enum.Font.GothamBold
ToggleBtn.Text = "ON"
ToggleBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
ToggleBtn.TextSize = 20
ToggleBtn.Draggable = true
ToggleBtn.Active = true
ToggleBtn.AutoButtonColor = false
ToggleBtn.BackgroundTransparency = 0.1

UICorner.CornerRadius = UDim.new(0, 25)
UICorner.Parent = ToggleBtn
UIStroke.Thickness = 3
UIStroke.Color = Color3.fromRGB(255, 255, 255)
UIStroke.Parent = ToggleBtn

-- ==================== KHỞI TẠO ANTI-BAN ====================
AntiBan:Init()

-- ==================== GIAO DIỆN CHÍNH ====================
local Window = Fluent:CreateWindow({
    Title = "Saidepchiu Hub - By Biendepzai",
    SubTitle = "Blox Fruits 2026 | Level 2800 | Anti-Ban Active",
    TabWidth = 160,
    Size = UDim2.fromOffset(680, 580),
    Acrylic = true,
    Theme = "Dark",
    MinimizeKey = Enum.KeyCode.LeftControl
})

local Tabs = {
    Farm = Window:AddTab({ Title = "Farm", Icon = "swords" }),
    ESP = Window:AddTab({ Title = "ESP", Icon = "eye" }),
    Teleport = Window:AddTab({ Title = "Teleport", Icon = "map-pin" }),
    FruitRaid = Window:AddTab({ Title = "Fruit/Raid", Icon = "apple" })
}

local Options = Fluent.Options
local playerLevel = 1
local playerFragments = 0
local playerBeli = 0
local currentSea = 1

local function updatePlayerStats()
    pcall(function()
        playerLevel = Player.Data.Level.Value
        playerFragments = Player.Data.Fragments.Value
        playerBeli = Player.Data.Beli.Value
        if Player.PlayerGui.Main.SeaIcon then
            local seaText = Player.PlayerGui.Main.SeaIcon.SeaText.Text
            if seaText:find("First") then currentSea = 1
            elseif seaText:find("Second") then currentSea = 2
            elseif seaText:find("Third") then currentSea = 3 end
        end
    end)
end

local function safeTP(pos)
    if Player.Character and Player.Character:FindFirstChild("HumanoidRootPart") then
        Player.Character.HumanoidRootPart.CFrame = pos
    end
end

local function bringTarget(target)
    pcall(function()
        for _, v in pairs(target:GetChildren()) do
            if v:IsA("Part") or v:IsA("MeshPart") or v:IsA("BasePart") then
                v.CFrame = Player.Character.HumanoidRootPart.CFrame * CFrame.new(0, 0, 3)
            end
        end
    end)
end

local function getQuestMobs(level)
    if level < 10 then return "Bandit"
    elseif level < 30 then return "Monkey"
    elseif level < 60 then return "Pirate"
    elseif level < 90 then return "Brute"
    elseif level < 120 then return "Desert Officer"
    elseif level < 150 then return "Snow Bandit"
    elseif level < 180 then return "Vampire"
    elseif level < 210 then return "Snow Troop"
    elseif level < 240 then return "Mercenary"
    elseif level < 270 then return "Swan Pirate"
    elseif level < 300 then return "Marine Captain"
    elseif level < 330 then return "Sky Bandit"
    elseif level < 360 then return "Dark Master"
    elseif level < 390 then return "Toga Warrior"
    elseif level < 420 then return "Gladiator"
    elseif level < 450 then return "Military Soldier"
    elseif level < 480 then return "Military Spy"
    elseif level < 510 then return "Fishman Warrior"
    elseif level < 540 then return "Fishman Commando"
    elseif level < 570 then return "God's Guard"
    elseif level < 600 then return "Shanda"
    elseif level < 630 then return "Royal Squad"
    elseif level < 660 then return "Royal Soldier"
    elseif level < 700 then return "Galley Pirate"
    elseif level < 725 then return "Raider"
    elseif level < 750 then return "Mercenary"
    elseif level < 775 then return "Swan Pirate"
    elseif level < 800 then return "Marine Captain"
    elseif level < 825 then return "Fishman Raider"
    elseif level < 850 then return "Fishman Captain"
    elseif level < 875 then return "Fishman Lord"
    elseif level < 900 then return "Pirate Raider"
    elseif level < 950 then return "Pirate Captain"
    elseif level < 1000 then return "Pirate Lord"
    elseif level < 1050 then return "Dark Raider"
    elseif level < 1100 then return "Dark Captain"
    elseif level < 1150 then return "Dark Lord"
    elseif level < 1200 then return "Raider (Lava)"
    elseif level < 1250 then return "Captain (Lava)"
    elseif level < 1300 then return "Lord (Lava)"
    elseif level < 1350 then return "Damaged Raider"
    elseif level < 1400 then return "Damaged Captain"
    elseif level < 1450 then return "Damaged Lord"
    elseif level < 1500 then return "Awakened Raider"
    elseif level < 1550 then return "Pirate Millionaire"
    elseif level < 1600 then return "Pirate Billionaire"
    elseif level < 1650 then return "Pirate Trillionaire"
    elseif level < 1700 then return "Forest Pirate"
    elseif level < 1750 then return "Musketeer Pirate"
    elseif level < 1800 then return "Mercenary"
    elseif level < 1850 then return "Swan Pirate"
    elseif level < 1900 then return "Marine Captain"
    elseif level < 1950 then return "Fishman Raider"
    elseif level < 2000 then return "Fishman Captain"
    elseif level < 2050 then return "Fishman Lord"
    elseif level < 2100 then return "Pirate Raider"
    elseif level < 2150 then return "Pirate Captain"
    elseif level < 2200 then return "Pirate Lord"
    elseif level < 2250 then return "Dark Raider"
    elseif level < 2300 then return "Dark Captain"
    elseif level < 2350 then return "Dark Lord"
    elseif level < 2400 then return "Raider (Lava)"
    elseif level < 2450 then return "Captain (Lava)"
    elseif level < 2500 then return "Lord (Lava)"
    elseif level < 2550 then return "Ghost Pirate"
    elseif level < 2600 then return "Cursed Sailor"
    elseif level < 2650 then return "Tyrant Soldier"
    elseif level < 2700 then return "Tyrant Captain"
    elseif level < 2750 then return "Tyrant General"
    elseif level < 2800 then return "Tyrant Lord"
    else return "Tyrant King" end
end

local bossList = {
    -- Sea 1
    "The Gorilla King", "Bobby", "Jeremy", "Diamond", "Saber Expert",
    "Greybeard", "Strawberry", "Cake Queen", "Don Swan", "Smoke Admiral",
    -- Sea 2
    "Wysper", "Darkbeard", "Rip_Indra", "Cursed Captain", "Ice Admiral",
    -- Sea 3
    "Dough King", "Rip_Return", "Unbound Werewolf", "Tiger Emperor",
    "Gas Boss", "Control Master"
}

-- ==================== AUTO FARM LEVEL ====================
local farmLoop
Tabs.Farm:AddSection("⚔️ AUTO FARM LEVEL 1-2800")
Tabs.Farm:AddToggle("AutoFarmLevel", {Title = "Tự động farm level", Default = false}):OnChanged(function(val)
    if val then
        Fluent:Notify({Title = "Farm", Content = "Bắt đầu farm level", Duration = 2})
        farmLoop = RunService.Heartbeat:Connect(function()
            pcall(function()
                updatePlayerStats()
                local mobName = getQuestMobs(playerLevel)
                local nearestMob = nil
                local nearestDist = math.huge
                
                for _, obj in pairs(Workspace:GetDescendants()) do
                    if obj:IsA("Model") and obj.Name == mobName and obj:FindFirstChild("Humanoid") and obj:FindFirstChild("HumanoidRootPart") then
                        local hum = obj:FindFirstChild("Humanoid")
                        if hum and hum.Health > 0 then
                            local dist = (obj.HumanoidRootPart.Position - Player.Character.HumanoidRootPart.Position).Magnitude
                            if dist < nearestDist then
                                nearestMob = obj
                                nearestDist = dist
                            end
                        end
                    end
                end
                
                if nearestMob and nearestDist < 2000 then
                    bringTarget(nearestMob)
                    safeTP(nearestMob.HumanoidRootPart.CFrame * CFrame.new(0, 0, 6))
                    task.wait(0.1)
                    VirtualUser:ClickButton1(Vector2.new(), Workspace.CurrentCamera.CFrame)
                end
            end)
        end)
    else
        if farmLoop then farmLoop:Disconnect() end
    end
end)

-- ==================== AUTO FARM ALL BOSS ====================
local allBossLoop
Tabs.Farm:AddToggle("AutoAllBoss", {Title = "Tự động farm tất cả boss", Default = false}):OnChanged(function(val)
    if val then
        Fluent:Notify({Title = "Boss", Content = "Bắt đầu farm all boss", Duration = 2})
        allBossLoop = RunService.Heartbeat:Connect(function()
            pcall(function()
                for _, bossName in ipairs(bossList) do
                    for _, obj in pairs(Workspace:GetDescendants()) do
                        if obj:IsA("Model") and obj.Name == bossName and obj:FindFirstChild("Humanoid") and obj:FindFirstChild("HumanoidRootPart") then
                            local hum = obj:FindFirstChild("Humanoid")
                            if hum and hum.Health > 0 then
                                bringTarget(obj)
                                safeTP(obj.HumanoidRootPart.CFrame * CFrame.new(0, 0, 8))
                                task.wait(0.1)
                                VirtualUser:ClickButton1(Vector2.new(), Workspace.CurrentCamera.CFrame)
                                break
                            end
                        end
                    end
                end
            end)
        end)
    else
        if allBossLoop then allBossLoop:Disconnect() end
    end
end)

Tabs.Farm:AddDropdown("BossSelect", {
    Title = "Chọn boss cần farm",
    Values = bossList,
    Default = 1
})

local selectedBossLoop
Tabs.Farm:AddToggle("AutoBoss", {Title = "Tự động farm boss (đã chọn)", Default = false}):OnChanged(function(val)
    if val then
        local bossName = Options.BossSelect.Value
        Fluent:Notify({Title = "Boss", Content = "Farm " .. bossName, Duration = 2})
        selectedBossLoop = RunService.Heartbeat:Connect(function()
            pcall(function()
                for _, obj in pairs(Workspace:GetDescendants()) do
                    if obj:IsA("Model") and obj.Name == bossName and obj:FindFirstChild("Humanoid") and obj:FindFirstChild("HumanoidRootPart") then
                        local hum = obj:FindFirstChild("Humanoid")
                        if hum and hum.Health > 0 then
                            bringTarget(obj)
                            safeTP(obj.HumanoidRootPart.CFrame * CFrame.new(0, 0, 8))
                            task.wait(0.1)
                            VirtualUser:ClickButton1(Vector2.new(), Workspace.CurrentCamera.CFrame)
                            break
                        end
                    end
                end
            end)
        end)
    else
        if selectedBossLoop then selectedBossLoop:Disconnect() end
    end
end)

Tabs.Farm:AddSection("⚡ FARM ĐẶC BIỆT (SỰ KIỆN)")
Tabs.Farm:AddToggle("AutoFactory", {Title = "Tự động đánh nhà máy", Default = false})
Tabs.Farm:AddToggle("AutoPiratesRaid", {Title = "Tự động đánh tập kích hải tặc", Default = false})
Tabs.Farm:AddToggle("AutoRipIndra", {Title = "Tự động đánh rip_indra", Default = false})
Tabs.Farm:AddToggle("AutoDarkbeard", {Title = "Tự động đánh darkbeard", Default = false})
Tabs.Farm:AddToggle("AutoDoughKing", {Title = "Tự động đánh dough king", Default = false})
Tabs.Farm:AddToggle("AutoRipReturn", {Title = "Tự động đánh Rip_Return (Update 28)", Default = false})
Tabs.Farm:AddToggle("AutoUnboundWerewolf", {Title = "Tự động đánh Unbound Werewolf (Update 28)", Default = false})
Tabs.Farm:AddToggle("AutoElfEvent", {Title = "Tự động farm Elf (Christmas Event 2025)", Default = false})

-- ==================== ESP PRO ====================
local ESP = {}
local espLoop

local function clearESP()
    for _, v in pairs(ESP) do
        pcall(function() 
            if v.Line then v.Line:Remove() end
            if v.Text then v.Text:Remove() end
            if v.Highlight then v.Highlight:Destroy() end
        end)
    end
    ESP = {}
end

local function createESP(obj, color, name, objType)
    local espObj = {}
    
    local highlight = Instance.new("Highlight")
    highlight.Adornee = obj
    highlight.FillColor = color
    highlight.FillTransparency = 0.5
    highlight.OutlineColor = Color3.fromRGB(255, 255, 255)
    highlight.OutlineTransparency = 0
    highlight.Parent = CoreGui
    espObj.Highlight = highlight
    
    local billboard = Instance.new("BillboardGui")
    billboard.Size = UDim2.new(0, 150, 0, 50)
    billboard.Adornee = obj
    billboard.AlwaysOnTop = true
    billboard.Parent = CoreGui
    
    local text = Instance.new("TextLabel")
    text.Size = UDim2.new(1, 0, 1, 0)
    text.BackgroundTransparency = 1
    text.Text = name
    text.TextColor3 = Color3.fromRGB(255, 255, 255)
    text.TextStrokeColor3 = Color3.fromRGB(0, 0, 0)
    text.TextStrokeTransparency = 0
    text.TextSize = 14
    text.Font = Enum.Font.GothamBold
    text.Parent = billboard
    
    espObj.Billboard = billboard
    espObj.Text = text
    espObj.Object = obj
    espObj.Type = objType
    
    table.insert(ESP, espObj)
    return espObj
end

local function createTracer(obj, color, name)
    local line = Drawing.new("Line")
    line.Visible = false
    line.Color = color
    line.Thickness = 2
    line.Transparency = 1
    
    local text = Drawing.new("Text")
    text.Visible = false
    text.Color = Color3.fromRGB(255, 255, 255)
    text.Center = true
    text.Outline = true
    text.Size = 16
    text.Font = 3
    
    table.insert(ESP, {Line = line, Text = text, Object = obj, Name = name, Color = color, Type = "Tracer"})
    return {Line = line, Text = text}
end

Tabs.ESP:AddSection("🔍 ESP PRO (HIGHLIGHT + TRACER + DISTANCE)")
Tabs.ESP:AddToggle("ESPBloxFruit", {Title = "Định vị trái blox", Default = false}):OnChanged(function(val)
    if val then
        espLoop = RunService.RenderStepped:Connect(function()
            for _, obj in pairs(Workspace:GetDescendants()) do
                if obj.Name:find("Fruit") or obj.Name:find("Banana") or obj.Name:find("Apple") then
                    local exists = false
                    for _, v in pairs(ESP) do
                        if v.Object == obj then exists = true break end
                    end
                    if not exists then
                        createTracer(obj, Color3.fromRGB(255, 215, 0), "🍎")
                    end
                end
            end
            for _, v in pairs(ESP) do
                if v.Type == "Tracer" and v.Object and v.Object.Parent then
                    local pos, onScreen = Workspace.CurrentCamera:WorldToViewportPoint(v.Object.Position)
                    if onScreen then
                        local center = Vector2.new(Workspace.CurrentCamera.ViewportSize.X / 2, Workspace.CurrentCamera.ViewportSize.Y)
                        v.Line
